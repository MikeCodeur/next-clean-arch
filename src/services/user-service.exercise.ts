import {AddUser, UpdateUser} from '@/types/user-types'

export async function createUserService(data: AddUser) {
  // 🐶 Implémente correctement en appellant le repo et ajoute des règles de validations simple si necessaire
}
